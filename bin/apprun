#!/bin/bash
#
# apprun
#
# Start server using Flask
#

# import app to allow the exe to be ran else where
from app import app

# Stop on errors
set -Eeuo pipefail

# Set Variables
export FLASK_APP=app 
FLASK_APP=app 
flask run --host 0.0.0.0 --port 8000